# Simple-ATM
Mini ATM Simulator A simple cash withdrawal simulator built with Asynchronous JavaScript(Promises & Async/Await) to simulate real-time transaction processes.

**🚀Main Features:**
- Input Validation: Checks for sufficient balance or invalid input (below 0).
- Real-Time Simulation: Uses a delay function (Promise) to simulate a banking process for 3 seconds.
- Error Handling: Uses a `try...catch...finally` block to ensure the button returns to normal after a transaction is completed or failed.

**🛠️Stack**
- HTML5 & CSS3
- Vanilla JavaScript.
